# ImRTon's coding space

Personal blog powered by [Jekyll](https://jekyllrb.com/) and the [Minimal Mistakes](https://mademistakes.com/work/minimal-mistakes-jekyll-theme/) theme (via `remote_theme`).

## Local preview

```bash
bundle install
bundle exec jekyll serve
```
